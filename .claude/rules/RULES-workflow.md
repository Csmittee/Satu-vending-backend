# RULES-workflow.md — Satu 1.0
> Version 1.1 — 2026-06-19
> Changes: Added R-115 moved from RULES.md
> Previous: v1.0 — 2026-06-11
> Domain: Session structure, CC prompt patterns, handoff discipline, three-repo system
> Load this file when: Starting any session · writing a CC prompt · planning session mode
---

## Three-Repo + Session Closing (newest first)
- R-84: Session closing discipline — every CC session ends:
         archive prompt → append RULES.md → update PROJECT_STATE.md →
         commit all docs → merge to main.
         CHAT_HANDOFF.md = Chat's job only — CC never writes it (R-05).
         No session closes without this sequence complete.
- R-83: Three-repo system — Chat and CC must read all three repos before any decision:
         backend (Satu-vending-backend), firmware (Satu-Vending-Firmware),
         hardware (Satu-vending-hardware). Hardware repo = wiring + BOM reference only.
         Never modify hardware repo wiring/BOM files. hardware.h R2 LOCKED in all contexts.

## Workflow Rules
- R-53: Bug tickets require: MODULE · FILE · SYMPTOM · CONSOLE output · NETWORK response · EXPECTED
- R-52: Chat gives file + line + old text + new text for Direct Override (no CC, no PR, 2 min)
- R-51: CC reads source files fresh from repo — no download/upload cycle needed
- R-50: CC_PROMPT files live at repo ROOT while active — owner pushes to root, CC reads from root.
         After CC executes and merges: CC moves file to docs/prompts/ stamped ✅ COMPLETE.
         docs/prompts/ = archive only. CC never looks there for a file to execute.
- R-104: CC_PROMPT files are always at repo ROOT while active. Owner pushes to root → tells CC to
          execute by filename → CC reads from root. After merge: CC moves to docs/prompts/ stamped
          ✅ COMPLETE — archive only. CC must NEVER search docs/prompts/ for a file to execute.

## Fix Escalation (moved from RULES.md 2026-06-19)
- R-115: CRITICAL FIX ESCALATION — after 2 failed fix loops on same symptom: STOP ALL CODE CHANGES.
         Step 1: List every file touched. List unverified assumptions. State original problem in one sentence.
         Step 2: Add serial output at exact failure point. Measure raw values (rc=, bytes=, PSRAM=YES/NO).
         Step 3: Search Arduino forums, GitHub issues, library release notes for exact error + version.
         Step 4: Fix must work in ALL modes (fake + live). A fix that only works in fake mode is a workaround.
         See SKILL_problem_solving_kt.md for full KT IS/IS-NOT framework.

## Session Protocol
- R-05: Every session ends with CHAT_HANDOFF.md generated by Chat (overwrite, not append)
- R-04: One session = one topic — max 2 bugs OR 1 feature, never mixed
- R-03: Build Mode = all docs + all affected source files (high token, CC creates PR)
- R-02: Fix Mode = CLAUDE.md + RULES.md + 1-2 source files only (low token)
- R-01: CC reads CLAUDE.md + RULES.md + PROJECT_STATE.md before touching any file — state names aloud
